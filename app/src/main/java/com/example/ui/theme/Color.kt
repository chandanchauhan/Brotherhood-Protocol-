package com.example.ui.theme

import androidx.compose.ui.graphics.Color

// Brotherhood Protocol Core Color Palette
val Soil = Color(0xFF2C1810)       // Main background dark
val Mud = Color(0xFF4A2F1A)        // Card background dark / surface dark
val Clay = Color(0xFF8B5E3C)       // Muted brown accent
val Wheat = Color(0xFFD4A853)      // Elegant gold accent / primary accent
val Sun = Color(0xFFF2C45A)        // Bright gold highlight
val Cream = Color(0xFFFAF3E0)      // Main text / backgrounds light
val Leaf = Color(0xFF3D6B3F)       // Rural advantages / positive items green
val Sky = Color(0xFF5B9BD5)        // Sleep / tech neutral blue
val Flame = Color(0xFFC0392B)      // Intensity / testosterone crimson
val Smoke = Color(0xFF6B7280)      // Slate gray / subtle text
val DarkCharcoal = Color(0xFF1E100A)// Deep outline / subtle shadow

// Light Theme Adaptations
val LightBackground = Color(0xFFFAF5E8)  // Clean, warm cream book/parchment background
val LightSurface = Color(0xFFF2EAD8)     // Slightly darker warm cream card surface
val LightOnSurface = Color(0xFF2C1810)   // Soil text on light surfaces
val LightBorder = Color(0xFFE4D6B8)      // Muted border lines for light theme
